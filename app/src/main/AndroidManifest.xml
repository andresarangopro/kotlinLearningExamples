<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.kotlinapp">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".DataStorage.SharedPreferences">
        </activity>
        <activity android:name=".SecondApp.RecyclerViewActivity">
        </activity>
        <activity android:name=".SecondApp.ListAdapterActivity">
        </activity>
        <activity
                android:name=".menu.Main2Activity"
                android:label="@string/title_activity_main2"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".menu.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".SecondApp.SecondApp"
                android:label="@string/title_activity_second_app"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity
                android:name=".FirstApp.WhatsMyName"
                android:label="@string/title_activity_firts_app"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>